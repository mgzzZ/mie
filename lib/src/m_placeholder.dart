import 'package:flutter/material.dart';

class MPlaceholder extends StatelessWidget {
  const MPlaceholder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
